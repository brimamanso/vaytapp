<div class="row">
    <div class="col-xs-12">
        <div class="col-lg-4 col-lg-offset-3 well"> 
            <%= simple_form_for([@faq, @reply]) do |f| %>
                <%= f.input :content, label: "Edit Comment:"%>
                <%= f.button :submit, class: "btn btn-primary" %>
            <% end %>
        </div>
    </div>
</div>